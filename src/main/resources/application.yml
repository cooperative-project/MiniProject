server:
  port:8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3308/test
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 5339
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update